# This file contains all of the interactive shell settings that are not
# specific to one shell.

# Set the EDITOR variable so `sudoedit` uses my Neovim setup.
export EDITOR='nvim'

# Setup pyenv to be used
eval "$(pyenv init -)"
